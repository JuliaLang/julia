# -*- mode: makefile -*-
## All `_jll` names and versions

# Automatic include of all `*.version` makefile
#
# Note: The following dependencies do not have a `_jll` pkg:
#	- libwhich
#	- patchelf
#	- utf8proc
include $(JULIAHOME)/deps/*.version
