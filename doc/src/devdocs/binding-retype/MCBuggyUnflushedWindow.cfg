\* TLC config for MCBuggyUnflushedWindow: the heavy fence omits the window
\* flush, so the drain can miss an in-flight commit window. Expect TypeSlotOK
\* (and, in deeper traces, ClearWriteFlagsSound / StoreValidation) to be
\* violated.
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT TypeSlotOK
INVARIANT ClearWriteFlagsSound
INVARIANT StoreValidation
CHECK_DEADLOCK FALSE
