┌ Info: a context specific log message
└ @ Main.__doctest__284 none:2
┌ Error: doctest failure in src/stdlib/Logging.md:238-267
│ 
│ ```jldoctest
│ # Load the logging module
│ julia> using Logging
│ 
│ # Open a textfile for writing
│ julia> io = open("log.txt", "w+")
│ IOStream(<file log.txt>)
│ 
│ # Create a simple logger
│ julia> logger = SimpleLogger(io)
│ SimpleLogger(IOStream(<file log.txt>), Info, Dict{Any,Int64}())
│ 
│ # Log a task-specific message
│ julia> with_logger(logger) do
│            @info("a context specific log message")
│        end
│ 
│ # Write all buffered messages to the file
│ julia> flush(io)
│ 
│ # Set the global logger to logger
│ julia> global_logger(logger)
│ SimpleLogger(IOStream(<file log.txt>), Info, Dict{Any,Int64}())
│ 
│ # This message will now also be written to the file
│ julia> @info("a global log message")
│ 
│ # Close the file
│ julia> close(io)
│ ```
│ 
│ Subexpression:
│ 
│ global_logger(logger)
│ 
│ Evaluated output:
│ 
│ ConsoleLogger(IOBuffer(data=UInt8[...], readable=false, writable=false, seekable=false, append=false, size=0, maxsize=0, ptr=1, mark=-1), ReentrantLock(), Info, Base.CoreLogging.default_metafmt, true, 0, Dict{Any, Int64}())
│ 
│ Expected output:
│ 
│ SimpleLogger(IOStream(<file log.txt>), Info, Dict{Any,Int64}())
│ 
│ # This message will now also be written to the file
│ 
│   diff = Warning: Diff output requires color.
SimpleLogger(IOStream(<file log.txt>), ConsoleLogger(IOBuffer(data=UInt8[...], readable=false, writable=false, seekable=false, append=false, size=0, maxsize=0, ptr=1, mark=-1), ReentrantLock(), Info, Dict{Any,Int64}())

# This message will now also be written to the fileBase.CoreLogging.default_metafmt, true, 0, Dict{Any, Int64}())
└ @ Documenter /home/namansoni/Desktop/GSoC/julia/doc/src/stdlib/Logging.md:238
┌ Error: doctest failure in src/stdlib/Logging.md:238-267
│ 
│ ```jldoctest
│ # Load the logging module
│ julia> using Logging
│ 
│ # Open a textfile for writing
│ julia> io = open("log.txt", "w+")
│ IOStream(<file log.txt>)
│ 
│ # Create a simple logger
│ julia> logger = SimpleLogger(io)
│ SimpleLogger(IOStream(<file log.txt>), Info, Dict{Any,Int64}())
│ 
│ # Log a task-specific message
│ julia> with_logger(logger) do
│            @info("a context specific log message")
│        end
│ 
│ # Write all buffered messages to the file
│ julia> flush(io)
│ 
│ # Set the global logger to logger
│ julia> global_logger(logger)
│ SimpleLogger(IOStream(<file log.txt>), Info, Dict{Any,Int64}())
│ 
│ # This message will now also be written to the file
│ julia> @info("a global log message")
│ 
│ # Close the file
│ julia> close(io)
│ ```
│ 
│ Subexpression:
│ 
│ @info("a global log message")
│ 
│ Evaluated output:
│ 
│ [ Info: a global log message
│ 
│ Expected output:
│ 
│ 
│ # Close the file
│ 
│   diff = Warning: Diff output requires color.

# Close the file[ Info: a global log message
└ @ Documenter /home/namansoni/Desktop/GSoC/julia/doc/src/stdlib/Logging.md:238
